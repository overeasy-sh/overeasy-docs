---
title: 'Join Agent'
description: 'The `JoinAgent` combines the results of multiple `Agents`.'
icon: 'robot'
---

## Properties

### Parameters

The `JoinAgent` does not accept arguments: 

```
JoinAgent()
```

### Example

Here is an example of the BoundingBoxSelectAgent as part of a `Workflow` for counting hardhats.

```python example.py
workflow.add_step(BoundingBoxSelectAgent(classes=["construction hat"], model=GroundingDINO(), split=True))
```

<CardGroup cols={2}>
  <Card
    title="Construction Example"
    icon="circle-info"
    href="https://overeasy.sh/docs/examples/construction-example"
  >
    See Full Example
  </Card>
</CardGroup>
