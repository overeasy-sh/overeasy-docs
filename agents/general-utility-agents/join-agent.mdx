---
title: 'Join Agent'
description: 'The `JoinAgent` combines the results of multiple `Agents`.'
icon: 'robot'
---

### Overview

The `JoinAgent` is often used in conjunction with the `SplitAgent`. It merges multiple splits of an image into a single node, which is useful in scenarios where you may want to consolidate results from different images after a split.